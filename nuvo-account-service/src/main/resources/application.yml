server:
  port: 8082

spring:
  application:
    name: account-service
  datasource:
    # Base de datos DIFERENTE: nuvo_account_db
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5444}/${DB_NAME:nuvo_account_db}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:1234}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        # dialect removed as it is auto-detected