server:
  port: 8084

spring:
  application:
    name: loan-service
  datasource:
    url: jdbc:postgresql://localhost:5444/nuvo_loan_db
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect